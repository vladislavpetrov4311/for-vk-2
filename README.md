# Задача 2
### Инструкция по запуску
* `git clone`
* `yc managed-kubernetes cluster get-credentials <имя_кластера> --zone <зона>`
* `kubectl apply -f conf.yaml`

### Чтобы убедиться, что всё действительно работает, могу в свой dev-стенд прокинуть Ваш public key, через ssh подключитесь к моему серверу, где всё настроено. 
### Если есть желание, можем созвониться и обсудить решение.